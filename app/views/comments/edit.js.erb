var modalBackground = $(".modal-background.edit-comment");

var containerModal = modalBackground.find(">:first-child");

var containerInfo = containerModal.find("div.edit-info-comment");

modalBackground.fadeIn(300);
containerModal.css("transform", "scale(1)");

containerInfo.html("<%= j render '/comments/form_edit', comment: @comment %>");
