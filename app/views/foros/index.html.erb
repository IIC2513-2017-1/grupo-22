<p id="notice"><%= notice %></p>

<h1> Foros</h1>

<div class='lista'>
  <ol>
    <% @foros.each do |f| %>
      <li>
      <%= link_to f.title, foro_path(f)%>
      </li>
    <% end %>
  </ol>
</div> 
<br>