<h1 style="color: #FFD700;font-weight: bolder;"> Foro </h1>

<p>
  <strong>Title:</strong>
  <%= @foro.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @foro.description %>
</p>
<p>
<h1 style="color: #FFD700;font-weight: bolder;">Topics</h1>

<% @foro.topics.each do |t| %>
  <p>
    <strong>Titulo:</strong>
    <%= link_to t.title, foro_topic_path(t) %>
  </p>
 
  <p>
    <strong>Description:</strong>
    <%= t.content %>
  </p>
   <i style="color: gray;">
    Created by <%= t.user.username %>
  </i>
  <hr>
<% end %>
</p>
<p>
	<% if current_user %>
		<h1 style="color: #FFD700; font-weight: bolder;">Add a Topic as  <%= current_user.username %></h1>
		<%= form_for([@foro, @foro.topics.build]) do |f| %>
		 <p>
		    <%= f.label :title %><br>
		    <%= f.text_field :title %>
		  </p>
		  <p>
		    <%= f.label :content %><br>
		    <%= f.text_area :content %>
		  </p>
		  <p>
		    <%= f.submit %>
		  </p>
		<% end %>
	<% end %>
</p>

<%= link_to 'Back', tourneys_path %>