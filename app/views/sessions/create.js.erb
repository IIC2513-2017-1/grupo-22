var modalBackground = $(".modal-background.login");

var containerModal = modalBackground.find(">:first-child");

var containerInfo = containerModal.find("div.info-login");

containerInfo.html( "<%= j render 'sessions/login' %>")
$("#alerta .notification").html("<%= flash[:alert] %>");
$("#noticia .notification").html("");