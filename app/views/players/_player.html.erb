<tr class="no-link" id="player_<%= player.id  %>">
  <td>
  <% if player.photo.present? %>
    <%= cl_image_tag(player.photo.url(:thumb)) %>
  <% else %>
    <%= image_tag("/default/thumb/missing.png", class:'fotothumb') %>
  <% end %>
  </td>
  <td><%= player.full_name %></td>
  <td><%= player.id %></td>
  <td><%= player.email %></td>
  <td class="value" id="cell-link">
    <%= link_to(team_player_path(player.team, player)) do %>
      <span class="icon-go2" style="margin-right: 5px;"></span>Go
    <% end %>
  </td>
  <% if current_user%>
    <% if @team && current_user.id == @team.user.id %>
      <td class="value" id="cell-link">
        <%= link_to(team_player_path(@team,player), remote: true, method: :delete, data: { confirm: 'Are you sure?' }) do %>
          <span class="icon-go2" style="margin-right: 5px;"></span>Remove
        <% end %>
      </td>
    <% end %>
  <% end %>
</tr>
